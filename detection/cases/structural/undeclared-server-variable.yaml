openapi: 3.1.0
info:
  title: Detection case
  version: "1.0.0"
servers:
  - url: https://{region}.example.com
paths:
  /things:
    get:
      operationId: listThings
      responses:
        "200":
          description: ok
