openapi: 3.1.0
info:
  title: Detection case
  version: "1.0.0"
paths:
  /things:
    get:
      operationId: listThings
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    properties:
                      a:
                        type: string
                  - type: object
                    properties:
                      a:
                        type: string
