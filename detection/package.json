{
  "name": "@oaverify-dev/detection",
  "version": "0.0.0",
  "private": true,
  "description": "Detection corpus: which OpenAPI defects each linter catches (oaverify vs ajv, Spectral, Redocly).",
  "type": "module",
  "scripts": {
    "detect": "tsx run.ts",
    "typecheck": "tsc --noEmit",
    "check": "pnpm typecheck"
  },
  "devDependencies": {
    "@redocly/cli": "^2.46.2",
    "@stoplight/spectral-cli": "^6.16.3",
    "ajv": "^8.20.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.3",
    "yaml": "^2.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
