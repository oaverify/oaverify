{
  "name": "oaverify",
  "version": "6.1.0",
  "description": "Command-line OpenAPI validator (3.0/3.1/3.2): validate requests, responses, and examples against a spec, resolve and lint documents, and compile standalone validators. Library API lives in @oaverify/core.",
  "keywords": [
    "api-validation",
    "cli",
    "command-line",
    "oas",
    "openapi",
    "openapi-3.0",
    "openapi-3.1",
    "openapi-3.2",
    "openapi-cli",
    "openapi-validator",
    "request-validator",
    "response-validator",
    "spec-validation",
    "swagger",
    "validation",
    "validator",
    "yaml"
  ],
  "homepage": "https://github.com/oaverify/oaverify#readme",
  "bugs": {
    "url": "https://github.com/oaverify/oaverify/issues"
  },
  "license": "MIT",
  "author": "Andrew Houghton <aah@roarmouse.org>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oaverify/oaverify.git",
    "directory": "packages/oav"
  },
  "bin": {
    "oaverify": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "exports": {
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "prepack": "node -e \"if((process.env.npm_config_user_agent||'').startsWith('npm/'))throw Error('Use pnpm pack: npm pack does not rewrite workspace:* deps.')\" && tsup && node ../../scripts/pack-devdeps.mjs strip",
    "postpack": "node ../../scripts/pack-devdeps.mjs restore"
  },
  "dependencies": {
    "@oaverify/check": "workspace:*",
    "@oaverify/core": "workspace:*",
    "@oaverify/stream": "workspace:*",
    "@oaverify/yaml": "workspace:*",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@oaverify/internal-cli": "workspace:*",
    "@oaverify/internal-spec": "workspace:*",
    "tsup": "8.5.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  },
  "peerDependencies": {
    "esbuild": "^0.28.0"
  },
  "peerDependenciesMeta": {
    "esbuild": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=22"
  },
  "//": "No prepublishOnly: release.yml runs `pnpm test` from root, then packs with `pnpm pack` and publishes each tarball with `npm publish <tarball>`, which does not run prepublishOnly at all. Per-package publish (`cd packages/<x> && pnpm publish`) is not the supported path; it would skip the test gate."
}
